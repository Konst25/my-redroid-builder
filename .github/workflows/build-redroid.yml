name: Build Redroid 11 with Houdini and GApps

on:
  workflow_dispatch:
    description: 'Сборка Redroid Android 11.0.0 с GApps и Houdini (ARM-трансляция)'

env:
  ANDROID_VERSION: "11.0.0"
  IMAGE_NAME: "my-redroid"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout redroid-script
        uses: actions/checkout@v4
        with:
          repository: ayasa520/redroid-script
          path: redroid-script

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y lzip

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Python dependencies
        working-directory: ./redroid-script
        run: |
          python -m pip install --upgrade pip
          pip install tqdm requests

      - name: Build Android 11 with OpenGapps and Houdini
        working-directory: ./redroid-script
        run: |
          echo "🔨 Сборка Android 11.0.0 с OpenGapps и Houdini ARM-трансляцией"
          echo "⚠️  ВНИМАНИЕ: Согласно документации, Houdini может не работать на Redroid"
          python redroid.py \
            -a 11.0.0 \
            -g \
            -i \
            -c docker

      - name: List Docker images
        run: |
          echo "=== Список всех Docker образов ==="
          docker images
          echo "=== Поиск образа redroid ==="
          docker images | grep redroid || echo "Образы redroid не найдены"

      - name: Find created image
        id: find_image
        run: |
          CREATED_IMAGE=$(docker images --format "{{.Repository}}:{{.Tag}}" | grep "redroid/redroid.*11.0.0" | head -n 1)
          
          if [ -z "$CREATED_IMAGE" ]; then
            CREATED_IMAGE=$(docker images --format "{{.Repository}}:{{.Tag}}" | grep "redroid" | grep "11.0.0" | head -n 1)
          fi
          
          if [ -z "$CREATED_IMAGE" ]; then
            echo "❌ Образ не найден!"
            docker images
            exit 1
          fi
          
          echo "✅ Найден образ: $CREATED_IMAGE"
          echo "created_image=$CREATED_IMAGE" >> $GITHUB_OUTPUT

      - name: Verify Houdini installation
        run: |
          echo "=== Проверка установки Houdini ==="
          docker run --rm ${{ steps.find_image.outputs.created_image }} sh -c "ls -la /system/lib64/libhoudini.so 2>/dev/null && echo '✅ Houdini найден' || echo '❌ Houdini не найден'"
          docker run --rm ${{ steps.find_image.outputs.created_image }} sh -c "ls -la /system/lib/libhoudini.so 2>/dev/null && echo '✅ Houdini (32-bit) найден' || echo '❌ Houdini (32-bit) не найден'"

      - name: Prepare registry
        id: registry
        run: |
          LOWERCASE_OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          echo "full_registry=ghcr.io/${LOWERCASE_OWNER}/${{ env.IMAGE_NAME }}" >> $GITHUB_OUTPUT

      - name: Set image tag
        id: tag
        run: |
          TAG="11.0.0-opengapps-houdini"
          echo "image_tag=${TAG}" >> $GITHUB_OUTPUT
          echo "Тег образа: ${TAG}"

      - name: Tag image for Registry
        run: |
          docker tag ${{ steps.find_image.outputs.created_image }} \
            ${{ steps.registry.outputs.full_registry }}:${{ steps.tag.outputs.image_tag }}
          docker tag ${{ steps.find_image.outputs.created_image }} \
            ${{ steps.registry.outputs.full_registry }}:11.0.0-houdini

      - name: Log in to Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Push to Registry
        run: |
          docker push ${{ steps.registry.outputs.full_registry }}:${{ steps.tag.outputs.image_tag }}
          docker push ${{ steps.registry.outputs.full_registry }}:11.0.0-houdini
          
          echo "✅ Образ опубликован: ${{ steps.registry.outputs.full_registry }}:${{ steps.tag.outputs.image_tag }}"

      - name: Cleanup
        run: |
          docker system prune -f
